# Production: same-origin (Go server serves SPA + API on the same port)
# Change to absolute URLs if frontend is deployed separately from backend:
#   VITE_API_URL=https://api.example.com/api
#   VITE_WS_URL=wss://api.example.com/ws
VITE_API_URL=/api
VITE_WS_URL=/ws
